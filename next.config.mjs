/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: { unoptimized: true },
  basePath: "/alehhu.github.io",
  assetPrefix: '/alehhu.github.io/',
  /**reactStrictMode: true,**/
  /**output: 'export',
  images: { unoptimized: true },**/
};

export default nextConfig;
